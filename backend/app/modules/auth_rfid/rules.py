def list_rules() -> list[str]:
    return [
        "Operator must be active",
        "Workstation must be active",
        "Every production action should be tied to an active work session",
    ]

