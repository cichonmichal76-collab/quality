def list_rules() -> list[str]:
    return [
        "Every uploaded package should have a checksum",
        "File storage should be abstracted away from API endpoints",
    ]
"""Miejsce na przyszle reguly walidacji uploadow."""
