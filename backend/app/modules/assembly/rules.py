def list_rules() -> list[str]:
    return [
        "Only validated components may be installed",
        "A component may not be installed into more than one active device",
        "Assembly should build a digital device tree",
    ]

